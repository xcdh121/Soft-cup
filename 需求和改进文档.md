# 万径（EduAgent）阶段收费与管理员端需求及改进文档

> 文档版本：V1.0  
> 编写日期：2026-08-03  
> 适用范围：当前已完成 MVP、进入决赛后的商业化与运营增强版本  
> 依据：仓库现有 `REQUIREMENTS.md`、前后端源码、数据库模型及迁移记录

## 1. 文档目的

本文档用于定义下一阶段的两项核心建设：

1. 增加 29.9 元、59.9 元、79.9 元三档阶段收费套餐，并对 AI 对话、文档、学习资源生成、多智能体运行等高成本能力进行可解释、可追溯的额度控制。
2. 增加独立管理员端，使管理员能够查看平台经营概况、用户与套餐、Agent 运行状态、课程内容、订单与审计记录。

本文档不只描述页面，还给出与当前代码相匹配的数据模型、接口、权限、改造文件、迁移步骤、测试和验收标准，可直接作为后续开发依据。

## 2. 已确认的项目现状

### 2.1 当前技术与产品基础

当前项目已经具备以下可复用能力：

| 能力 | 当前实现 | 对本次改造的价值 |
| --- | --- | --- |
| 用户与登录 | 自托管 JWT；`users` 已有 `is_active`、`is_admin` | 可直接作为管理员权限基础 |
| 用户管理 | 已有管理员创建、查询列表、删除用户接口 | 扩展状态、套餐、封禁和详情即可 |
| 使用量 | `user_usage` 记录聊天、闪卡、测验、思维导图、文档上传的每日次数 | 可作为迁移期兼容数据，不宜继续承担完整计费 |
| 限额拦截 | `UsageService.check_and_increment()` 已接入聊天及部分生成接口 | 可替换为统一权益与额度服务 |
| 课程库 | 已有课程、章节、知识点、关系、课程资源 CRUD | 管理员端无需重写课程模块 |
| 多智能体 | 已有 Supervisor、Profile、KT、Diagnosis、Resource、Planner 等编排能力 | 可直接观测和统计 |
| Agent 留痕 | 已有 `agent_runs`、`agent_events`、`skill_executions`、`agent_tool_calls`、`agent_artifacts` | 管理员观测台已有主要数据源 |
| 前端 | React 19、TypeScript、TanStack Router、Effect、Tailwind | 可新增计费和 `/admin` 路由分支 |
| 后端 | FastAPI、SQLAlchemy、Alembic、PostgreSQL | 适合增加订单、权益、额度流水和审计表 |

### 2.2 当前主要缺口

1. 额度由环境变量统一配置，不能按用户套餐动态变化。
2. 当前按“每天次数”累计，没有套餐有效期、购买记录、剩余额度、冻结额度和额度流水。
3. 当前部分操作在任务真正成功前就计数，失败任务也可能消耗次数。
4. 没有价格页、购买页、订单、支付回调、退款或人工赠送权益。
5. 管理员后端只有基础用户接口，前端没有管理员路由和菜单。
6. Agent 接口只能由所属用户按 `run_id` 查询详情，不能跨用户分页查询或做汇总。
7. `agent_runs` 没有心跳、模型名称、Token、推理成本等运营字段。
8. 课程当前按 `owner_id` 私有隔离，缺少“平台公共课程—发布—学生可见”的内容运营流程。
9. 当前课程 `status` 是普通字符串，缺少严格的草稿、已发布、已下架状态约束。
10. `UsageService` 支持思维导图默认额度，但 `Settings`/依赖注入没有完整传入该配置，改造时应一并修正。

## 3. 本阶段目标与边界

### 3.1 产品目标

1. 用户能够清楚看到三个套餐的价格、权益、有效期和剩余额度。
2. 高成本操作必须经过服务端权益校验，不能通过绕过前端直接调用接口超额使用。
3. 支付成功或管理员赠送后，权益能够幂等到账。
4. 管理员可以在一个后台中完成日常用户、Agent、课程和订单运营。
5. 所有管理员写操作与计费变化都可审计、可追溯。
6. 在不破坏现有 MVP 主流程的前提下渐进迁移。

### 3.2 本期不做

1. 暂不做复杂的自动续费、按年订阅和家庭共享。
2. 暂不做课程单独售卖、优惠券、分销和多商户结算。
3. 暂不做模型按 Token 向用户实时计价；Token 与成本只作为内部核算指标。
4. 暂不对外展示模型思维链、完整 Prompt、密钥、原始工具参数或内部堆栈。

### 3.3 收费模式假设

本方案默认采用“单次购买、30 天有效、到期不自动续费”的阶段权益包。原因是实现和合规复杂度低，适合决赛版本快速形成真实闭环。

如果产品最终要求按自然月自动续费，只需替换支付协议与续费任务；套餐、权益、额度桶和流水模型仍可继续使用。正式开发支付前，应由产品负责人书面确认：有效期、是否自动续费、退款口径和发票主体。

## 4. 用户与权限角色

### 4.1 角色定义

| 角色 | 权限 |
| --- | --- |
| 游客 | 查看产品介绍与套餐，不可使用学习能力 |
| 注册用户 | 使用试用权益；查看自己的订单、套餐和额度 |
| 付费用户 | 在有效权益和额度内使用相应能力 |
| 运营管理员 | 用户查询、权益赠送、课程运营、Agent 查看、订单查询 |
| 超级管理员 | 运营管理员全部能力，并可管理管理员、套餐配置和敏感系统配置 |

### 4.2 权限实现建议

当前 `users.is_admin` 足够支撑决赛版，可先把管理员统一视为超级管理员。后续如果管理员人数增加，再增加 `role` 或 `user_roles` 表，拆分 `operator`、`content_admin`、`finance_admin`、`super_admin`。

所有 `/api/v1/admin/**` 接口必须使用后端 `require_admin` 校验。前端隐藏菜单只是交互优化，不能作为安全措施。

## 5. 阶段收费模块需求

### 5.1 套餐建议

以下额度是首版运营建议值，不是永久商业承诺。上线后应根据真实的 Token、OCR、图片生成、PPT 和第三方接口成本每周复盘。

| 权益 | 体验版 | 基础版 | 进阶版（推荐） | 专业版 |
| --- | ---: | ---: | ---: | ---: |
| 售价 | 0 元 | 29.9 元 | 59.9 元 | 79.9 元 |
| 有效期 | 注册后 7 天 | 30 天 | 30 天 | 30 天 |
| AI 导师消息 | 30 次 | 300 次 | 800 次 | 1500 次 |
| 文档上传/解析 | 3 份 | 20 份 | 60 份 | 120 份 |
| 测验生成 | 3 次 | 20 次 | 60 次 | 120 次 |
| 闪卡生成 | 3 次 | 20 次 | 60 次 | 120 次 |
| 思维导图生成 | 1 次 | 10 次 | 30 次 | 60 次 |
| 多智能体完整运行 | 2 次 | 15 次 | 50 次 | 100 次 |
| 个性化资源包 | 1 个 | 5 个 | 20 个 | 40 个 |
| 活跃学习项目 | 1 个 | 3 个 | 10 个 | 30 个 |
| 文件存储上限 | 200 MB | 1 GB | 3 GB | 5 GB |
| 高级诊断与动态路径 | 体验 | 支持 | 支持 | 支持 |
| 任务优先级 | 普通 | 普通 | 较高 | 最高 |

套餐内部应使用稳定代码，而非中文名称：

- `trial`
- `basic_2990`
- `advanced_5990`
- `pro_7990`

金额统一存储为整数分：`2990`、`5990`、`7990`，禁止用浮点数保存金额。

### 5.2 额度口径

1. 额度按权益包有效期累计，不再按自然日重置。
2. 同一用户可以拥有多个未过期权益包；能力等级取最高档，次数额度按额度桶累加。
3. 消耗时优先使用最早到期的额度桶，减少用户损失。
4. 用户主动取消、服务端失败、队列失败、第三方失败时不得扣减最终额度。
5. 执行流程采用“校验 → 预占 → 执行 → 成功确认/失败释放”，避免并发超扣和失败扣费。
6. 用户重复提交必须携带 `idempotency_key`，同一业务动作只能记一笔消耗。
7. 管理员可赠送或扣回额度，但必须填写原因并写入审计日志。
8. 套餐到期后，历史学习内容保留并允许查看；仅阻止新的高成本操作，不能把用户数据锁死。
9. 项目数和存储量属于容量型限制，不适用“次数预占”，应在创建或上传前实时检查。

### 5.3 计费失败提示

后端应返回稳定错误码，前端据此展示升级弹窗：

| 场景 | HTTP 状态 | 业务码 |
| --- | --- | --- |
| 没有有效权益 | 402 | `PAYMENT_REQUIRED` |
| 当前能力不属于该套餐 | 403 | `FEATURE_NOT_INCLUDED` |
| 某项额度已用完 | 429 | `QUOTA_EXCEEDED` |
| 项目/存储达到容量限制 | 409 | `CAPACITY_LIMIT_REACHED` |

建议错误响应：

```json
{
  "error": {
    "code": "QUOTA_EXCEEDED",
    "message": "本期多智能体运行额度已用完",
    "resource_type": "agent_run",
    "used": 15,
    "limit": 15,
    "reset_at": "2026-09-02T08:00:00Z",
    "upgrade_url": "/pricing"
  }
}
```

### 5.4 购买与到账流程

```text
用户选择套餐
  → 后端读取启用中的套餐并创建订单快照
  → 后端向支付渠道创建支付单
  → 前端展示二维码/收银台
  → 支付渠道异步通知后端
  → 后端验签、校验金额、幂等更新订单
  → 创建用户权益和额度桶
  → 写入额度流水与管理员审计/系统事件
  → 前端轮询或通过事件刷新套餐状态
```

安全要求：

1. 价格、有效期、额度均由服务端读取，不能相信前端传来的金额。
2. 支付回调必须验签，并以 `provider_transaction_id` 和通知 ID 做唯一约束。
3. 支付回调即使重复到达，也只能到账一次。
4. 原始支付通知只保存必要字段或脱敏摘要，不记录密钥。
5. 订单超时关闭后，如果出现延迟成功通知，必须进入人工核对或按照渠道规则恢复订单，不能静默丢单。
6. 决赛演示环境可先支持 `manual` 支付渠道，由管理员确认到账；生产环境再实现 `wechat_pay`/`alipay` 适配器。演示接口不得在生产环境启用。

### 5.5 订单状态

```text
created → pending_payment → paid
created/pending_payment → closed
paid → refunding → refunded
refunding → refund_failed
```

状态只能由服务端状态机推进。已支付订单不能被普通“删除”接口物理删除。

### 5.6 用户端页面

1. `/pricing`：三档套餐对比、有效期、常见问题、购买按钮。
2. `/dashboard/billing`：当前套餐、到期时间、各项已用/剩余、订单历史。
3. 设置页增加“套餐与额度”入口，保留当前的工具调用统计。
4. 额度不足弹窗展示：缺少的能力、当前使用量、推荐套餐、前往购买。
5. 支付结果页展示“处理中/成功/失败”，不能只以浏览器跳转结果判断支付成功。

## 6. 管理员端需求

### 6.1 管理员首页

路由：`/admin`

首屏指标：

- 注册用户数、活跃用户数、付费用户数。
- 今日订单数、实收金额、退款金额、套餐分布。
- 今日 Agent 运行数、成功率、失败率、平均耗时、P95 耗时。
- 今日 LLM 输入/输出 Token、估算成本、单付费用户平均成本。
- 文档解析队列、运行中任务、超时任务和异常告警数。
- 已发布课程、草稿课程、下架课程数量。

所有指标应支持日期范围和时区，默认按 `Asia/Shanghai` 展示；数据库时间继续使用 UTC。

### 6.2 Agent 运行观测

路由：`/admin/agent-runs`

列表字段：运行 ID、用户、项目、目标、当前 Agent、状态、开始时间、持续时间、最后心跳、Token、估算成本、错误摘要。

筛选条件：时间、状态、Agent 名称、用户、项目、目标、是否超时、是否使用 fallback。

详情内容：

1. 基础上下文摘要，不展示敏感原文。
2. 事件时间线。
3. Skill 执行、工具调用、耗时、结果摘要、fallback 和错误码。
4. 产物列表及其业务对象链接。
5. Token 和成本明细。
6. 管理员操作：复制追踪 ID、标记已处理、对失败任务重试。

“取消运行”需谨慎实现：当前任务若在进程内同步执行，无法可靠强杀。建议增加 `cancellation_requested_at`，由编排器在 Agent/工具边界协作式检查；只有支持幂等的失败任务才允许重试。

告警建议：

- `running` 且 2 分钟无心跳：标记 `suspected_stuck`。
- 运行时间超过业务阈值：标记超时。
- 5 分钟内同类错误超过阈值：管理员首页告警。
- 单次成本超过阈值：成本异常告警。

### 6.3 课程管理

路由：`/admin/courses`

管理员应能够：

1. 创建、编辑、复制、归档课程。
2. 管理章节树、拖拽排序、学习目标和预计时长。
3. 管理知识点、难度、标签与先修关系。
4. 添加课程资源：文档、链接、视频、代码、题库、生成资源。
5. 将资源关联到章节和多个知识点。
6. 预览课程，执行发布前校验。
7. 发布、下架和恢复课程。
8. 查看课程使用人数、学习项目数、资源完成情况和常见薄弱知识点。

发布前校验：课程名称、至少一个章节、章节至少一个知识点、关键资源可访问、外链格式合法、版权/许可信息已填写。已被用户使用的课程不允许物理删除，只能归档或下架。

现有课程是“用户私有课程”。本次应增加 `visibility=private/platform` 与 `publish_status=draft/published/unpublished/archived`。普通用户：

- 只能编辑自己的私有课程。
- 可以读取管理员已发布的平台课程。
- 不能修改平台课程；需要个性化时应“复制到我的课程”。

### 6.4 用户与权益管理

路由：`/admin/users`

管理员可以按用户名、注册时间、账户状态、套餐、到期时间搜索；查看用户的项目数、存储量、额度使用、订单和近期 Agent 运行；启用/停用账户；人工赠送套餐或额度；重置密码。

不建议继续提供无保护的“删除用户”作为常用操作。默认应停用账户；真正删除需二次确认、说明数据影响，并满足个人信息删除流程。

### 6.5 订单与退款

路由：`/admin/orders`

支持订单号、用户、套餐、状态、渠道、渠道流水号、时间范围筛选；查看回调与状态变更摘要；对异常订单人工标记；发起退款并记录原因。

财务金额不得在前端自行汇总后作为唯一依据，聚合应由后端基于已支付/已退款状态计算。

### 6.6 套餐配置

路由：`/admin/billing-plans`

管理员可以调整新订单使用的套餐说明、排序、展示状态和额度。已支付订单必须保留购买时的套餐快照，后续修改套餐不能追溯改变历史权益。

价格修改、套餐下架和人工扣减额度属于高风险操作，必须二次确认并记录操作者、原因、前后值、IP 和时间。

### 6.7 审计日志

路由：`/admin/audit-logs`

记录范围：管理员登录、用户状态变更、角色变更、权益赠送/扣回、订单人工处理、退款、课程发布/下架、套餐修改、Agent 重试/取消。

审计日志只允许查询和归档，不允许通过业务接口修改或删除。

## 7. 数据模型改进

### 7.1 新增计费表

#### `billing_plans`

- `id`
- `code`：唯一稳定代码
- `name`
- `price_cents`
- `currency`：默认 `CNY`
- `duration_days`
- `quotas`：JSON，保存各资源额度
- `features`：JSON，保存能力开关与任务优先级
- `is_active`
- `sort_order`
- `created_at`、`updated_at`

#### `payment_orders`

- `id`、`order_no`（唯一）
- `user_id`、`plan_id`
- `plan_snapshot`：下单时价格、期限、额度和功能快照
- `amount_cents`、`currency`
- `provider`
- `provider_transaction_id`（渠道内唯一）
- `status`
- `expires_at`、`paid_at`、`refunded_at`
- `created_at`、`updated_at`

#### `payment_events`

- `id`、`order_id`
- `provider_event_id`（唯一）
- `event_type`
- `payload_digest`、`verified`
- `processed_at`、`created_at`

#### `user_entitlements`

- `id`、`user_id`、`order_id`、`plan_id`
- `plan_snapshot`
- `status`：`pending/active/expired/revoked/refunded`
- `starts_at`、`ends_at`
- `granted_by_admin_id`、`grant_reason`
- `created_at`、`updated_at`

#### `quota_buckets`

- `id`、`user_id`、`entitlement_id`
- `resource_type`
- `granted`、`used`、`reserved`
- `starts_at`、`expires_at`
- 唯一约束：`entitlement_id + resource_type`
- 检查约束：所有数值非负，且 `used + reserved <= granted`

#### `quota_ledger`

- `id`、`user_id`、`bucket_id`
- `resource_type`
- `operation`：`grant/reserve/commit/release/adjust/refund/expire`
- `quantity`
- `business_type`、`business_id`
- `idempotency_key`（唯一）
- `operator_id`、`reason`
- `created_at`

### 7.2 管理与审计表

#### `admin_audit_logs`

- `id`、`admin_user_id`
- `action`、`target_type`、`target_id`
- `before_snapshot`、`after_snapshot`
- `reason`、`request_id`、`ip_address`
- `created_at`

### 7.3 课程表扩展

对 `courses` 增加：

- `visibility`：`private/platform`
- `publish_status`：`draft/published/unpublished/archived`
- `published_at`、`published_by`
- `version`
- `cover_url`
- `enrollment_count` 可动态聚合，不建议作为首版强一致字段

`status` 与 `publish_status` 不应长期并存两个含义相近的字段。迁移期可把现有 `status=active` 映射为 `publish_status=draft` 或根据数据来源人工确认，随后弃用旧字段。

### 7.4 Agent 表扩展

对 `agent_runs` 增加：

- `current_agent_name`
- `heartbeat_at`
- `duration_ms`
- `model_name`
- `input_tokens`、`output_tokens`
- `estimated_cost_micros`：以人民币或美元百万分之一为统一内部单位
- `trace_id`
- `cancellation_requested_at`
- `retry_of_run_id`
- `handled_at`、`handled_by`

索引建议：

- `(status, created_at)`
- `(user_id, created_at)`
- `(project_id, created_at)`
- `(heartbeat_at, status)`
- `trace_id` 唯一索引

## 8. 后端接口设计

### 8.1 用户计费接口

```text
GET  /api/v1/billing/plans
GET  /api/v1/billing/me
GET  /api/v1/billing/me/quotas
GET  /api/v1/billing/orders
POST /api/v1/billing/orders
GET  /api/v1/billing/orders/{order_no}
POST /api/v1/billing/orders/{order_no}/close
POST /api/v1/billing/payment-notify/{provider}
```

`POST /payment-notify/{provider}` 是渠道回调，不使用普通用户 JWT，必须使用渠道验签。

### 8.2 管理员接口

```text
GET   /api/v1/admin/overview

GET   /api/v1/admin/users
GET   /api/v1/admin/users/{user_id}
PATCH /api/v1/admin/users/{user_id}
POST  /api/v1/admin/users/{user_id}/entitlements
POST  /api/v1/admin/users/{user_id}/quota-adjustments

GET   /api/v1/admin/orders
GET   /api/v1/admin/orders/{order_no}
POST  /api/v1/admin/orders/{order_no}/refund

GET   /api/v1/admin/agent-runs
GET   /api/v1/admin/agent-runs/{run_id}
POST  /api/v1/admin/agent-runs/{run_id}/retry
POST  /api/v1/admin/agent-runs/{run_id}/cancel
PATCH /api/v1/admin/agent-runs/{run_id}/handling

GET   /api/v1/admin/courses
POST  /api/v1/admin/courses
PATCH /api/v1/admin/courses/{course_id}
POST  /api/v1/admin/courses/{course_id}/publish
POST  /api/v1/admin/courses/{course_id}/unpublish
POST  /api/v1/admin/courses/{course_id}/archive

GET   /api/v1/admin/billing-plans
POST  /api/v1/admin/billing-plans
PATCH /api/v1/admin/billing-plans/{plan_id}
GET   /api/v1/admin/audit-logs
```

所有列表接口必须分页，推荐统一使用 `page/page_size` 或 cursor，不应一次返回全表。当前 `GET /api/v1/users` 返回全部用户，应在管理员端改用新的分页接口。

### 8.3 现有接口改造

1. `GET /api/v1/usage` 暂时保留，响应中增加当前套餐、有效期和额度周期；前端迁移完成后统一到 `/billing/me/quotas`。
2. 聊天、文档、测验、闪卡、思维导图的调用点，由 `UsageService.check_and_increment()` 改为 `QuotaService.reserve/commit/release()`。
3. 多智能体诊断、推荐、学习路径和资源包也必须接入额度校验；当前这些能力尚未完整纳入用量计数。
4. 课程读取接口允许“本人拥有”或“平台已发布”，写接口仍要求本人拥有或管理员权限。
5. 当前 `/api/v1/agent-runs/{run_id}` 保持用户本人可见；管理员跨用户查看使用独立 `/admin/agent-runs`，避免无意放宽原接口。

## 9. 代码修改建议

### 9.1 数据库层

修改：`src/shared/db/src/edu_db/models.py`

- 增加套餐、订单、支付事件、权益、额度桶、额度流水、管理员审计模型。
- 扩展课程和 Agent 字段及关系。
- 金额用 `Integer`，成本用整数微单位；不要用 `Float`。
- JSON 字段只存快照或灵活策略，需检索和强约束的字段必须独立成列。

新增 Alembic 迁移：

- 创建新表、索引、唯一约束、检查约束和外键。
- 初始化四个套餐。
- 为现有用户发放一次兼容性试用权益，避免发布后立即全部被拦截。
- 课程发布字段回填后，再分阶段废弃旧 `status` 语义。

当前 Alembic head 为 `5c2f8a1d7e40`，新迁移应以实际分支最新 head 为 `down_revision`，不要手工改旧迁移。

### 9.2 核心服务层

建议新增：

```text
src/shared/core/src/edu_core/services/billing.py
src/shared/core/src/edu_core/services/quotas.py
src/shared/core/src/edu_core/services/admin_dashboard.py
src/shared/core/src/edu_core/services/admin_audit.py
src/shared/core/src/edu_core/schemas/billing.py
src/shared/core/src/edu_core/schemas/admin.py
```

核心职责：

- `BillingService`：套餐、订单、支付状态机、权益到账、退款。
- `QuotaService`：读取有效额度桶，原子预占、确认、释放和流水。
- `AdminDashboardService`：聚合用户、订单、Agent、课程数据。
- `AdminAuditService`：统一记录管理员敏感操作。

额度并发处理必须在数据库事务中使用行锁或原子条件更新，例如更新条件包含 `used + reserved + quantity <= granted`，不能先查余额再在另一个事务扣减。

### 9.3 API 层

建议新增：

```text
src/edu-api/routers/billing.py
src/edu-api/routers/admin.py
src/edu-api/payment/providers/base.py
src/edu-api/payment/providers/manual.py
src/edu-api/payment/providers/wechat_pay.py   # 生产阶段
src/edu-api/payment/providers/alipay.py       # 生产阶段
```

修改：

- `src/edu-api/main.py`：注册 billing/admin router。
- `src/edu-api/dependencies.py`：注入 Billing、Quota、Audit 服务；修正思维导图额度配置遗漏。
- `src/edu-api/auth.py`：继续复用 `require_admin`；后续再扩展细粒度角色。
- `src/edu-api/exception_handlers.py`：增加稳定业务码和 402/403/409/429 计费错误结构。
- `src/edu-api/config.py`、`.env.example`：增加支付渠道、回调地址、演示模式、成本阈值配置，密钥只能从环境变量或密钥服务读取。

### 9.4 现有业务调用点

优先替换已存在计数的文件：

```text
src/edu-api/routers/chats.py
src/edu-api/routers/documents.py
src/edu-api/routers/quizzes.py
src/edu-api/routers/flashcard_groups.py
src/edu-api/routers/mind_maps.py
```

随后补齐尚未统一计数的多智能体、诊断、推荐、学习路径和资源包入口。推荐封装 FastAPI dependency/context manager，避免每个 router 手写不同的扣减逻辑。

旧 `user_usage` 在一个发布周期内保留双读或只读兼容；确认新额度流水稳定后停止写入，最后再单独迁移删除，避免一次发布同时改表、改接口和改前端导致回滚困难。

### 9.5 Agent 运行数据采集

修改编排服务与 ToolRunner：

- 每进入/离开 Agent 更新 `current_agent_name` 和 `heartbeat_at`。
- 每次模型响应累计 input/output Token。
- 根据模型价格配置计算 `estimated_cost_micros`，价格表需带生效时间。
- 运行结束写入 `duration_ms`。
- 错误对管理员保存错误码和脱敏摘要，对普通用户只返回可理解提示。
- 取消标记在 Agent、Skill、工具边界检查；写操作重试必须复用幂等键。

不要在管理员端展示隐藏思维链。可展示的是路由决策摘要、证据引用、工具结果摘要、耗时和错误。

### 9.6 课程服务

修改：

```text
src/shared/core/src/edu_core/services/courses.py
src/shared/core/src/edu_core/services/course_resources.py
src/shared/core/src/edu_core/schemas/courses.py
src/edu-api/routers/courses.py
```

把当前 `_get_owned_course()` 拆为：

- `_get_readable_course(course_id, user_id)`：本人私有课程或已发布平台课程。
- `_get_writable_course(course_id, user_id, is_admin)`：本人私有课程或管理员管理的平台课程。

发布/下架使用显式服务方法，不允许通过普通 PATCH 任意写状态，从而确保校验、时间、操作者和审计完整。

### 9.7 前端

建议新增：

```text
src/edu-web/src/features/billing/**
src/edu-web/src/features/admin/dashboard/**
src/edu-web/src/features/admin/agent-runs/**
src/edu-web/src/features/admin/courses/**
src/edu-web/src/features/admin/users/**
src/edu-web/src/features/admin/orders/**
src/edu-web/src/features/admin/audit-logs/**
src/edu-web/src/data-acess/billing.ts
src/edu-web/src/data-acess/admin.ts
src/edu-web/src/routes/admin-*.tsx
```

修改：

- `src/edu-web/src/routes/_config.tsx`：增加 `/pricing`、`/dashboard/billing`、`/admin/**`；增加 `requireAdmin` 路由守卫。
- `src/edu-web/src/components/navigation/app-sidebar.tsx`：仅管理员显示“管理后台”；普通用户显示“套餐与额度”。
- `src/edu-web/src/features/settings/settings-page.tsx`：将当前每日额度升级为权益周期额度，并显示到期时间。
- `src/edu-web/src/lib/auth-client.ts`：已包含 `is_admin`，可直接用于界面路由判断，但进入页面后仍以服务端 403 为最终结果。
- 生成式 API 的错误处理统一识别 `PAYMENT_REQUIRED`、`QUOTA_EXCEEDED`，弹出同一个升级组件。

当前 `agent-runtime-page.tsx` 更像面向学生的能力展示/观测页。管理员 Agent 监控应新建真实数据页面，不应直接把该展示页改成后台，以免混合学生视角与内部运营数据。

## 10. 非功能需求

### 10.1 安全

1. 管理员接口后端强制鉴权，默认拒绝。
2. 支付密钥、JWT 密钥、模型密钥不得进入前端、日志或数据库快照。
3. 管理员查看 Agent 数据时对聊天正文、文件名、工具参数和错误信息脱敏。
4. 管理员敏感操作需要原因、二次确认和审计。
5. 支付回调需防重放、验签、校验金额和商户号。
6. 生产环境关闭开发认证绕过和 manual 支付确认接口。

### 10.2 性能与可靠性

1. 管理员列表接口 P95 小于 1 秒，详情 P95 小于 2 秒（不含第三方实时调用）。
2. 额度预占与确认必须具备事务一致性，不能出现负余额。
3. 支付重复通知 10 次只产生一个权益。
4. 管理员首页使用聚合查询或短时缓存，避免每次请求扫描全部事件表。
5. Agent 事件表按 `run_id + created_at` 建索引；数据量增长后再按月归档。

### 10.3 可观测性

至少记录：请求 ID、用户 ID、订单号、run ID、trace ID、业务错误码、耗时、Token、估算成本。禁止记录密码、支付密钥、完整 Authorization 和模型隐藏推理。

## 11. 实施阶段

### 阶段 A：额度与管理员只读闭环（决赛优先）

1. 新增套餐、权益、额度桶、流水和审计表。
2. 初始化 29.9/59.9/79.9 套餐。
3. 替换现有固定每日额度为套餐额度。
4. 完成用户套餐页、额度不足弹窗。
5. 完成管理员首页、Agent 列表/详情、用户详情只读页面。
6. 支持管理员手工发放套餐，形成无需真实支付渠道的完整演示。

### 阶段 B：课程运营

1. 增加平台课程可见性和发布状态。
2. 实现管理员课程、章节、知识点、资源和发布流程。
3. 普通用户可浏览平台课程并复制到“我的课程”。
4. 增加课程使用与学习效果指标。

### 阶段 C：真实支付

1. 接入一个正式支付渠道。
2. 完成创建支付单、验签回调、幂等到账、订单查询和关单。
3. 完成退款、异常订单和对账处理。
4. 完成生产密钥与回调安全配置。

### 阶段 D：运营增强

1. Agent 心跳、卡死检测、成本告警、幂等重试和协作式取消。
2. 细粒度管理员角色。
3. 套餐转化漏斗、成本和毛利分析。
4. 根据真实成本调整套餐额度。

## 12. 测试要求

### 12.1 单元测试

- 三档套餐与到期逻辑。
- 多额度桶按最早到期顺序消耗。
- 并发预占不会超扣。
- 业务成功确认、失败释放、重复幂等键不重复扣费。
- 支付状态机禁止非法跳转。
- 重复支付通知不重复到账。
- 普通用户不能访问任何管理员接口。
- 平台课程的读写权限与发布校验。

### 12.2 集成测试

- 注册 → 试用 → 额度不足 → 购买/管理员赠送 → 权益到账 → 再次使用。
- 聊天、文档、测验、闪卡、思维导图、Agent、资源包的扣减闭环。
- 管理员查看 Agent 列表、详情、失败筛选和处理标记。
- 管理员创建课程 → 配置章节/知识点/资源 → 发布 → 普通用户可见。
- 已支付订单退款后权益和剩余额度按规则撤销。

### 12.3 回归测试

必须继续通过现有认证、用量、课程、聊天、Agent orchestration、资源包、测验、学习路径等测试。迁移发布前执行 Alembic upgrade/downgrade 演练和数据库备份恢复演练。

## 13. 核心验收标准

1. 三个套餐均由数据库配置并可在用户端正确展示，金额与后端一致。
2. 管理员发放或真实支付成功后，用户在 5 秒内可看到新权益。
3. 任意受控能力额度用完后，直接调用 API 也会被拦截。
4. 失败任务不消耗最终额度，重复请求不重复扣费。
5. 管理员能够按条件查看全平台 Agent 运行，并看到事件、Skill、工具、耗时和脱敏错误。
6. 2 分钟无心跳的运行可被管理员识别为疑似卡死。
7. 管理员能够创建并发布平台课程；普通用户只读，不能越权修改。
8. 普通用户访问 `/api/v1/admin/**` 全部返回 403。
9. 每次权益调整、退款、课程发布/下架、Agent 重试都有审计记录。
10. 重复支付通知不会重复创建权益或额度。

## 14. 上线与回滚建议

1. 先部署只建表、不启用拦截的数据库迁移。
2. 为现有用户生成兼容试用权益并核对数量。
3. 开启双写监控，对比旧 `user_usage` 与新额度流水，不立即删除旧表。
4. 通过功能开关逐项启用聊天、文档、生成、Agent 的额度拦截。
5. 最后开放购买入口；真实支付必须先在沙箱环境完成回调和重复通知测试。
6. 回滚时关闭新额度拦截开关，恢复旧固定额度读取；不要回滚或删除已产生的订单和支付流水。

## 15. 待产品确认事项

以下问题不影响先开发管理员只读和额度底座，但在接真实支付前必须确认：

1. 三档价格是 30 天单次权益包，还是自动月度订阅？
2. 体验版是 7 天试用还是永久低额度？
3. 套餐升级时是叠加额度、补差价，还是新套餐立即覆盖？本方案默认叠加额度、能力取最高档。
4. 退款后已使用额度如何处理？本方案建议只退未使用权益，特殊情况管理员人工处理。
5. 首个正式支付渠道选择微信支付还是支付宝？
6. 额度表中的首版数值是否满足实际模型和第三方服务成本？
7. 是否需要发票、未成年人保护、用户协议和隐私政策的专项页面？

## 16. 总结

本次改造的关键不是简单增加一个价格页面，而是建立“套餐—订单—权益—额度桶—额度流水—业务执行”的完整闭环，并用管理员端承接用户、Agent、课程和财务运营。

当前项目已经拥有管理员标记、基础用户管理、课程结构和完整度较高的 Agent 留痕，因此不需要推倒重来。最稳妥的路径是先复用现有模型和接口，新增独立计费底座与管理员聚合接口，再逐项替换固定每日限额。这样既能快速形成决赛展示亮点，也能为真实商业化保留一致性、安全和可扩展性。
