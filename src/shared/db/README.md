# edu-db

Dedicated database package for EduAgent.

Contains:

- SQLAlchemy `Base` and engine/session helpers (`edu_db.base`, `edu_db.session`)
- All ORM models (`edu_db.models`)
- Alembic env for migrations (`edu_db.alembic`)
